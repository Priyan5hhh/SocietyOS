# Needs Your Input

Maintained by Agent 4 (Reporter). Every item below is a stubbed button, mock integration,
or placeholder that was built so the surrounding feature works end-to-end, but needs a
real credential, account, or decision from you before it's production-ready.

| Phase | Item | What was stubbed | What you need to provide |
|-------|------|-------------------|---------------------------|
| 0 | Resident phone+OTP login (`requestResidentOtp`/`verifyResidentOtp` in `src/lib/services/auth.ts`) | The `/resident/login` screen (`src/routes/ResidentLogin.tsx`) implements the full phone → OTP → session UI flow, but no SMS provider is wired up. Codes are generated client-side, shown on-screen in a visible "DEV MODE" banner, and checked locally — nothing is actually texted, and the session created on success uses placeholder identity fields (`dev-resident` / `dev-unit` / `dev-society`) rather than a real resident lookup. | A real SMS/OTP provider (Twilio Verify, MSG91, AWS SNS, or Supabase's built-in phone-auth SMS provider) wired into the `societyos-api` backend behind `POST /api/auth/resident/otp/request` and `POST /api/auth/resident/otp/verify`, which should look up the resident by phone, issue a real code via the provider, and on verify return the resident's real `resident_id`/`unit_id`/`society_id` plus a real Supabase access/refresh token pair. Once that route exists, swap the stub implementations in `auth.ts` for real `fetch` calls to it (the request/response shapes are already documented in code comments there). |
| 0 | WhatsApp login-link affordance | Not built — Phase 0 ships phone+OTP only (per `docs/pipeline/phase0-research.md` §2/§3.3), no "send me a WhatsApp login link" button exists yet, so there is nothing stubbed here to wire up. Listed for visibility in case a later phase adds it. | WhatsApp Business API credentials, if/when that affordance is added — same phone number the society already messages residents on. |
| 0 | "Invite to portal" action (`src/admin/pages/ResidentRegistry.tsx`) | The admin-side button calls `POST /api/residents/:id/invite`, which doesn't exist yet on the `societyos-api` backend (that backend lives outside this repo). The Supabase schema changes it needs (`residents.auth_user_id`, `portal_invited_at`, `portal_activated_at` columns + resident-scoped RLS policies) were applied directly to the project via migration `resident_portal_auth_linkage`. | Implement `POST /api/residents/:id/invite` on the backend: set `portal_invited_at = now()` on the resident row and return the updated resident. No other backend logic is required for Phase 0 (no approval queue, per the research spec). |
| 1 | Resident "Pay Now" online checkout (`src/resident/Dues.tsx`, `payStep === "stub"`) | The full Pay Now UI flow is built (confirm amount → choose method), but the final charge step is a clearly-labeled "Online payments launching soon" panel with disabled UPI/Card/Net banking tiles instead of a real gateway checkout. It routes the resident to "I already paid" instead, which is a real, working Supabase write (see `resident_intimate_payment` RPC). | A payment-aggregator account (Razorpay/Cashfree or equivalent) — needs the society's bank account, PAN, and KYC documents for RBI aggregator onboarding, which an agent cannot obtain. Once you have live API keys: store them per-society (the `societies.razorpay_key_id` column already exists), wire the checkout SDK into the "Continue to pay" step, and implement a real `POST /api/billing/dues/:id/pay` + webhook handler on `societyos-api` that inserts into `payments` with `method='razorpay'` on success (the `payment_webhook_events` table is already there to receive it). |
| 1 | Resident session still uses Phase 0's dev-mode OTP (`src/lib/services/auth.ts`) | `verifyResidentOtp` now looks up the real resident by phone via the `lookup_resident_by_phone` Supabase RPC, so "My Dues" reads/writes real data for residents whose phone number matches a `residents.phone` row — but the session itself still isn't a real Supabase Auth session (fake `access_token`/`refresh_token`, no `auth.uid()`). Because of that, the new resident-facing RPCs (`resident_get_dues`, `resident_get_payments`, `resident_intimate_payment`) are `SECURITY DEFINER` functions scoped by the `unit_id`/`due_id` the frontend passes in, not by a verified JWT identity — anyone who knows a unit's id could call them for that unit. This is the same underlying gap as the Phase 0 OTP item above, now with real data behind it. | Once a real SMS/OTP provider is wired up (Phase 0 item) and `verifyResidentOtp` returns a real Supabase-issued JWT for the resident (via `residents.auth_user_id`), swap these RPCs' scoping from explicit `p_unit_id`/`p_due_id` params to `auth.uid()`-based checks against `residents.auth_user_id`, and add real RLS policies on `payments`/`dues` for the `resident` role instead of routing everything through RPC. |
| 1 | Admin "Mark paid (offline)" and resident "My Dues" write straight to Supabase, not `societyos-api` | Per the Phase 1 spec, these do NOT need external credentials and are fully functional today — `payments` schema extension, the `reconcile_offline_payment`/`settle_pending_payment`/`resident_*` RPCs, and the admin/resident UI in `src/admin/pages/Billing.tsx` and `src/resident/Dues.tsx` all work against the live Supabase project. Flagging only so the eventual `societyos-api` backend owner knows: if `POST /api/billing/dues/:id/reconcile` and `GET/POST /api/resident/dues`/`/intimate` are later built on that backend (per the research doc's endpoint contract), these frontend call sites should be migrated from direct `supabase-js` RPC calls to that backend, and the RPCs' `SECURITY DEFINER` grants to `anon`/`authenticated` should be revisited (tightened or removed) at that point. | No action needed unless/until `societyos-api` grows these routes — this is a heads-up, not a blocker. |
| 2 | Amenity booking, vendor/AMC, staff attendance, ticket cost/vendor/photo, admin intake review — all built directly against Supabase, no external credentials needed | Per the Phase 2 research doc's architecture finding, none of this needs a new third-party account — same reasoning as Phase 1's billing work above. New tables (`amenities`, `amenity_bookings`, `vendors`, `staff_attendance`) plus `tickets.vendor_id`/`resolution_cost`/`resolution_notes`/`intake_confidence`/`resident_match_status` and `ticket_attachments.stage` were applied via migrations `phase2_amenities_vendors_attendance` and `phase2_amenity_booking_functions`, with RLS scoped the same way as existing tables (admin/facility by `society_id` + role; residents through `SECURITY DEFINER` RPCs — `resident_list_amenities`, `resident_book_amenity`, `resident_cancel_amenity_booking`, `resident_list_amenity_bookings` — same pattern as `resident_get_dues`, because resident login is still the Phase 0 dev-mode OTP stub with no real Supabase JWT). Amenity/resolution photo uploads reuse the existing ImageKit pattern (`uploadImage(file, "amenity-photos" \| "ticket-photos")` in `src/lib/services/storage.ts`) — falls back to a mock object URL if `VITE_IMAGEKIT_AUTH_ENDPOINT` isn't deployed, same as resident/visitor photos. | No action needed. Once the real SMS/OTP provider from the Phase 0 item above is wired up and resident sessions carry a real Supabase JWT, the resident amenity RPCs should move to `auth.uid()`-scoped RLS instead of explicit `p_unit_id`/`p_resident_id` params, same follow-up as the Phase 1 billing RPCs. |
| 2 | Amenity paid-booking checkout is UI-only (no charge happens) | `src/resident/Amenities.tsx`'s booking modal shows the fee and creates the booking with `payment_status: 'pending'`, but there is no "pay now" step — same gap as Phase 1's dues checkout, and the same root cause (no payment-aggregator account exists yet). | Once a Razorpay/Cashfree account exists (see Phase 1's "Resident Pay Now" row above for what's needed), wire the amenity booking's `fee_amount > 0` case into the same checkout flow, scoped to `amenity_bookings.id` instead of `dues.id`. |
| 2 | Facility-manager attendance/vendor/amenity screens depend on a real Supabase JWT for staff, which already exists — no gap, but flagging the dependency | `getStaffClaims()` (`src/lib/services/supabase.ts`) decodes the staff session's JWT client-side to read `sub`/`app_metadata.society_id` for stamping `marked_by_staff_id`/`society_id` on inserts (avoids an extra `auth.getUser()` network round trip). This only works because staff/admin sign-in already issues a real Supabase-signed JWT (per Phase 0/1 notes) — resident sessions do NOT have this, which is why the resident-facing amenity screens go through RPCs instead. | No action needed — noting for whoever touches staff auth next that this decoding helper exists and should keep working as long as staff sign-in keeps issuing real Supabase JWTs. |
| 3 | Guest pre-approval (`src/resident/PreApprovals.tsx`), security signals (`src/admin/pages/SecuritySignals.tsx`), ledger/GST invoices (`src/admin/pages/Ledger.tsx`, Billing.tsx's "GST invoice"/"Apply late fees"), defaulter export (`src/admin/pages/Defaulters.tsx`), audit log (`src/admin/pages/AuditLog.tsx`), document vault + elections (`src/admin/pages/Documents.tsx`, `Elections.tsx`, `src/resident/Documents.tsx`, `src/resident/Vote.tsx`) — all built directly against Supabase, no external credentials needed | Per `docs/pipeline/phase3-research.md`, none of Phase 3's core build needs a new third-party account. New tables/columns (`security_flags`, `ledger_entries`, `gst_invoice_counters`, `gst_invoices`, `documents`, `unit_occupancy_history`, `elections`/`election_candidates`/`election_votes`, `society_configs.late_fee_rate_annual_pct`/`late_fee_grace_days`/`gstin`, `dues.late_fee_amount`/`late_fee_applied_at`, `defaulters` view) plus RPCs (`refresh_security_flags`, `review_security_flag`, `rebuild_ledger`, `apply_late_fees`, `generate_gst_invoice`, `resident_create_pre_approval`, `resident_list_pre_approvals`, `resident_list_documents`, `resident_list_elections`, `resident_list_candidates`, `cast_vote`, `election_results`) were applied via migrations `phase3_schema` and `phase3_functions`, with RLS/SECURITY DEFINER scoping following the same admin-JWT / resident-RPC-by-unit-id split as every prior phase. GST invoice numbering is sequential and gap-free per society+financial-year (`gst_invoice_counters`); ballot secrecy for elections rests on `unique(election_id, unit_id)` plus the resident session, not a fake e-signature UI, per the research doc's explicit call on that. Election "PDF"/GST-invoice "PDF" are rendered client-side (browser print / on-screen), not a generated PDF file, to avoid pulling in a PDF library for this pass. | No action needed for the DB/RPC layer. If a true downloadable PDF (rather than print-to-PDF) is wanted for GST invoices later, add a PDF-generation step (e.g. a small client-side library or an edge function) and populate `gst_invoices.pdf_imagekit_url`. |
| 3 | Guest pre-approval ships the resident half only — no gate-arrival push notification for unmatched walk-ins | The research doc's item 07 spec includes a `visitor_approval_requests` table + guard "notify resident, wait for approve/deny" flow for walk-ins (as opposed to guest pre-approvals created ahead of time, which is fully built). That half was not built this phase — prioritized breadth across all 7 build items over depth on any single one. `src/guard/pages/LogVisitor.tsx` still just logs unmatched visitors without notifying the resident. | No external credential needed — this is a scope cut, not a stub. Follow `docs/pipeline/phase3-research.md` §3.1 (`visitor_approval_requests` table + guard/resident UI) to complete it; it reuses the existing WhatsApp send path, no new integration. |
| 3 | Subscription self-serve "Update payment method" (`src/admin/pages/Subscription.tsx`) | "Change plan" is real (writes `subscriptions.plan_id` directly via Supabase, RLS-gated to admin/finance). "Update payment method" is a clearly-labeled "Launching soon" modal, same pattern as Phase 1's Pay Now stub — no live Razorpay/Stripe customer-vault integration exists yet to actually change a stored payment method. | Same payment-aggregator account as Phase 1's Pay Now item (Razorpay/Cashfree, needs the society's bank details + KYC, which an agent cannot obtain). Once live keys exist, wire this button into that gateway's customer/payment-method-update flow and write the result to `subscriptions.payment_method_label` (and whatever token/mandate field the gateway needs, which doesn't exist in the schema yet). |
| 3 | Payment gateway for automatic late-fee auto-charge | Late fee **calculation** is real and unstubbed (`apply_late_fees` RPC computes simple interest, capped at 21%/year per cooperative-society model bylaws, and writes `dues.late_fee_amount`). Auto-**charging** it (rather than adding it to the next bill for manual/offline payment) needs a saved payment method / mandate via Razorpay, which doesn't exist yet — same root cause as every other payment-gateway item in this file. | Once Phase 1's Razorpay integration is live, wire an auto-debit mandate flow for late fees; until then, land late fees as a ledger line item payable through the existing (already-stubbed) payment flow. |
| 4 | "Alert security desk" (SOS) button, Active Alerts screen, notice read receipts, resident PWA install — all built directly against Supabase, no external credentials needed | Per `docs/pipeline/phase4-research.md`, this phase needed no new third-party account. New table `sos_alerts` (+ RLS scoped admin/guard by `society_id`, same pattern as every prior phase) and RPCs `resident_raise_sos_alert`/`resident_get_sos_alerts` were applied via migrations `phase4_sos_alerts` and `phase4_sos_alerts_rpc_denormalize_server_side`. The resident button (`src/resident/ResidentHome.tsx`) requires an explicit two-step confirm (tap → modal → "Confirm — alert security desk") before writing, and every string says "Alert security desk," never "SOS"/"Emergency"/any dispatch-implying language, per the research doc's safety requirement. The guard/admin "Active Alerts" screen (`src/guard/pages/ActiveAlerts.tsx`, routed at both `/guard/alerts` and `/admin/alerts`) polls every 8s and supports Acknowledge/Resolve. Notice read receipts (`src/admin/pages/Notices.tsx`) read `notice_recipients.delivery_status` directly — this is WhatsApp's own delivery/read state (populated by `societyos-api` from WhatsApp Cloud API webhooks), not an in-app view event, since the resident portal has no notices-reading screen yet (per the research doc, that's a future item, not this phase's scope). PWA: added `public/resident-manifest.webmanifest` (`start_url: "/resident"`) alongside the existing guard one, extended `public/sw.js`'s precache list to cover `/resident` too, and `src/lib/registerSW.ts` now swaps the `<link rel="manifest">` href based on the page's initial path and registers the service worker for both `/guard` and `/resident`. | No action needed for the DB/RPC/UI layer. Same identity caveat as every other resident-facing RPC in this file: `resident_raise_sos_alert`/`resident_get_sos_alerts` are `SECURITY DEFINER` functions scoped by the ids the (still dev-mode OTP) resident session carries, not a verified JWT — once Phase 0's real SMS/OTP provider item is resolved and resident sessions carry a real Supabase JWT, migrate these to `auth.uid()`-scoped RLS like the rest. If/when a resident-facing notices screen is built, wire it to call a `resident_mark_notice_read`-style RPC (or set `notice_recipients.delivery_status='read'` directly) so in-app reads count as read receipts too — nothing to build until that screen exists. The manifest swap is a lightweight per-initial-path swap (not a full server-rendered role-aware manifest); if a user navigates from `/resident` to `/admin` in the same tab without a reload, the manifest tag won't update — acceptable since installs happen from whatever route the user is currently on. |
